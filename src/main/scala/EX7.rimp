arr := [1, 2, 3, 4, 5];
arr_len := 5;
left := 0;
right := !arr_len - 1;
while(!left < !right) do {
    tmp_left := arr[!left];
    tmp_right := arr[!right];
    arr[!right] := !tmp_left;
    arr[!left] := !tmp_right;
    left := !left + 1;
    right := !right - 1
}